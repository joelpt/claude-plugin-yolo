Invoke the `yolo` skill now and follow its instructions to execute autonomous work mode.

/loop 10 (silently: if we have been idle for more than about 8 minutes or if we hit any kind of api, rate, or usage limit error, retry last command now)
